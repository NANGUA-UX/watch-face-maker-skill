# 指针表盘

在指针表盘或数字与指针混合表盘中读取。本分支复用现有切图母件、背景和实例流程，不把动态指针烘焙进背景。

## 制作前锁定

- 区分时针、分针、秒针与数据指针。每根指针记录母件、局部轴心、表盘内轴心、零位、正方向、角度范围和遮挡顺序。
- 数据指针的目标值、起始角和扫过方向来自需求或开发配置，不能仅凭截图猜测真实目标。演示完成率应注明为演示；缺目标不能擅自视为100%。
- 数据子表盘默认沿顺时针数值递增，实际指针映射与刻度一致；不得因镜像排版或参考图画反而让其中一盘逆时针增长。只有用户明确要求其他方向时才覆盖此规则。目标值和起始角仍不能猜测。
- AOD按用户确认保留需要的指针。当前灰色描边规范为无填充、`#B3B3B3`；亮屏/AOD对应指针必须同轴、同角、同尺寸。数字AOD检查只适用于数字时间，不能要求纯指针表盘另外生成数字时间。

## 指针母件与旋转

- 母件标准姿态指向12点，切图宽高为正偶数整数，透明边界包含针尖、针尾、描边和阴影；轴心不一定在切图中心。裁剪后必须重算局部轴心并补偿实例定位。
- 每根指针是一张可导出资源，通过实例旋转。外观相同的数据指针复用母件，不按业务重复建图。
- 为方便在Figma手动调角度，每根指针显示实例单独套一个与所属表盘等宽高的FRAME（包括已有AOD及数据指针），命名如“旋转 / Hour”；它只用于旋转，不设填充、描边或导出，关闭裁切，不能将切图母件扩大成整屏。此类旋转框属于有用途的整屏容器，不按普通功能组收紧。
- 框内指针保持12点零位及1×尺寸，按局部轴心(px,py)放在(W/2-px,H/2-py)，由外框承担当前角度。外框中心必须与实际旋转轴心重合；数据指针按其小表盘轴心定位外框，不能一律放在主表盘中心。保留原实例ID、母件引用和层叠顺序，复合父子变换保持当前画面不动。主稿和缩略图随Active引用更新。
- 用户调整时选中外层“旋转 / …”画框修改角度，不旋转内部紧边切图实例。变换与层叠检查复合完整祖先链到Active/AOD坐标系，不把框内零位角当最终显示角；缺失祖先时不得宣称通过。画框本身超出表盘不等于指针越界，仍按指针实际扫过范围检查。位置标注继续描述原切图顶边至轴心，不能用旋转框尺寸替代。
- 按时针、分针、秒针由下到上组装。固定轴帽虽不变化，若需要盖在动态指针上，必须保留为前景资源；这类遮挡关系是静态内容不能一律合入最底层背景的原因。
- 表盘上的轴心坐标按整数布局。旋转矩阵产生的实例X/Y可以有小数，不四舍五入，否则转轴漂移。普通功能组、未旋转切图实例仍遵循整数规则。
- 新建或适配指针的局部轴心也取整数像素，使零位左上角和顶边至轴心标注为整数。缩放产生小数时，在偶数切图框内平移完整指针图形，将局部轴心对齐整数，并用同一偏移补偿亮屏、AOD及旋转实例；检查针尖、尾部、阴影未裁切。不能仅四舍五入标注，不能取整旋转矩阵。复用旧资源若轴心不合规，保留旧母件并创建目标规格，不能暗改已验收源盘。
- 指针内部的矢量路径保留原始曲线精度，不能套用文字框偶数尺寸规则。

角度以12点为0、顺时针为正。连续指针的时针角度为`(hour % 12) * 30 + minute / 2 + second / 120`，分针为`minute * 6 + second / 10`，秒针为`second * 6`。设备只支持离散更新时记录实际更新规则，不声称已实现连续运动。

## 切图区的位置标注

指针资源交付必须在Figma切图区提供可见的位置标注，不能仅在JSON或代码里记录pivot。按目标屏形、尺寸和圆角建立示意，放置标准朝12点的指针；通过现有切图母件的1×实例展示，不复制一套矢量或改变正式母件。需要AOD时并排展示对应实例，并注明并排偏移仅用于对照，实际定位仍与亮屏同轴。

- 红色竖向尺寸线从**切图外框顶边到旋转轴心**，值为局部轴心Y；不是整张切图高度，也不是可见针尖到轴心的长度。针尖、阴影和裁切留白可能使两者不同，不能照抄参考案例的数值。
- 同时写明资源名称或角色、实际切图宽高、局部轴心(px,py)、屏幕轴心(cx,cy)及标准姿态的切图左上角(cx-px,cy-py)。单位为px，坐标原点为各自左上角。使用当前母件和实例变换、明确的轴心几何或开发约定核对，不能默认轴心等于切图中心。
- 覆盖时分秒、用户要求的AOD指针及已有数据指针。相同母件用于不同子盘时复用资源，分别注明屏幕轴心；不为标注新增秒针、AOD或数据功能。
- 标注容器、尺寸线和说明不设导出，不混入背景、正式切图、Active、AOD或缩略图。保留原有母件ID与引用；位置标注的大圆屏只是开发示意，不把紧边指针资源改成整屏切图。
- 修改母件尺寸、裁切、轴心或实例定位后，更新受影响标注并回读核对；实例会同步外形，但手写尺寸文字不会自动更新。零位摆放和局部轴心应能重建屏幕轴心，再检查代表性的旋转姿态，避免只验标签存在。


## 数据子表盘的制作与排布

- 同层级、对称成对的数据展示采用共同的字号层级、对齐基准、间距和视觉样式，协调身份标识、数值/单位位置、圆心和刻度半径。内容量和语义不同可合理调整，但应在共同排布规则下说明理由，不能跳过完整模块检查。优先复用母件/变体/实例，按语义填入已有字段，不为对称编造单位、图标或新增数据。
- 数据数字、刻度数字、刻线、文字和该模块已有指针的轮廓与轴心均在Figma中用可编辑TEXT、矢量和组件制作；复杂装饰内容按[混合制作与素材质量](background-composition.md)选择原生或直接AI局部生成，指针表面素材由原生结构约束并随指针独立切出，不把材质静态误当成指针静态。先清除底图中将被重建的原内容，再放入原生元素，不在原图单位/刻度上叠加新字。功能图标继续优先使用官方库，缺失时单一纯色色块占位，不自行描摹。
- 保留可编辑的源结构。静态文字与刻度可按既有拆分规则合入背景PNG；实时数字与旋转指针仍为独立资源。Figma绘制不等于每个静态元素都要额外导出一张切图。
- 分开规划中心指针区、数据内容区和刻度环区。数据、标签、单位与刻度不能互相叠加，也不能压在高光边框、刻线或强纹理上难以辨识；同时检查未激活/激活样式和最长值。不能只把文字置顶来掩盖空间冲突。
- 顺时针检查使用同一圆心及起始角，验证递增值的角度顺序和指针姿态；不得只交换25/75文字而保留旧指针映射。整圆0与100%的表达另按需求确认，不擅自改变总量程。
- 同组刻度数字到对应刻线的可见间距保持一致。沿刻线方向测量实际字形边缘到刻线含描边边缘的距离，并让字形与对应刻线的中心轴对齐；不能只统一文字框中心半径或以“不重叠”代替等距检查。上下方向按字形高度、左右方向按字形宽度分别定位，文字位置按一位小数精度补偿。具体间距依本次设计决定，不写死通用像素值；收紧后同时复查与数据区的留白。
- 正常转动的时分秒针暂时经过数据区是可接受表现，不作为缺陷，也不为此新增隐藏、透明或避让行为。检查重点是静态持续重叠、错误层级以及数据和装饰之间的阅读冲突。

## 形状与材质分阶段检查

每根参考指针在绘制前记录针尖、收肩、内芯端部、镂空、连接段、针尾与轴帽的可信形状、比例和连接关系，标明可见、模糊或被遮挡。复杂或模糊处按[局部还原](design-rationality.md)采用与可信轮廓连贯的最小合理补全，不能把反光或灰边猜成额外零件；有明确依据的分件和非对称则保留。没有依据的精确尺寸不得冒充测量结果。

1. **结构先行**：对齐参考与实现的轴心、指向和显示比例，逐根比较临时纯色预览中的外轮廓及内部开口。被遮挡部分不计为已验证；分别记录针尖、收肩、内芯端部、连接段与针尾的差距。平切被画成尖角、宽金属框被改成细柄、孔洞被填实等可见结构差异未解决时，不进入材质阶段。相同总长或轴心不能证明形状接近，不能按实现的宽度单独拉伸参考来掩盖比例差异。
2. **材质复核**：结构通过后添加效果，核对金属明暗与材质边界，不能制造没有依据的台阶、拼接块或端帽。允许近似不清楚的质感，不借此改变可辨识轮廓；制作方法仍按混合制作规则选择。真实孔洞保留透明通道，不能用黑色块模拟。
3. **装配与派生**：在目标尺寸、放大局部与整盘复核形状、质感和连接，记录每层参考/实现图片或节点来源、实际差距、修改及验证状态。局部轮廓与材质复核后再组装正式实例、同步对应AOD及位置标注；保留母件ID和组件引用，裁切或尺寸改变时补偿局部轴心与实例定位。检查描边、阴影和孔洞边缘未被截断，清理临时预览。

几何、透明、旋转及AOD检查与形状对照分别记录；任何一项通过都不能代替其余项。新反馈指出结构问题后，重新检查该指针及受影响派生资源，不沿用整盘或旧版本的“通过”结论。具体尺寸与形状来自本次参考，不固定为某一种表盘模板。

## 实际检查

`scripts/pointer_checks.py`提供角度计算、绕指定轴心变换、母件引用、实际矩阵、完整圆周安全范围、层叠及AOD配对检查。使用时给快照增加：

```json
{
  "face_type": "analog",
  "pointers": [{
    "role": "Hour",
    "master_id": "实际母件ID",
    "instance_id": "实际实例ID",
    "pivot": [16, 140],
    "center": [240, 240],
    "angle": 304.3,
    "local_visible_bounds": [7, 5, 18, 165]
  }]
}
```

数值仅说明字段格式，不是后续表盘的固定模板。`local_visible_bounds`必须从实际PNG的透明通道读取；不能把计划矩形当实际图形。AOD配对角色为`HourDark`、`MinuteDark`。用户确认AOD只保留时分针时，不额外显示秒针、数据或轴帽；其他AOD方案按实际要求检查。

至少验证12/24小时等价时间、整点、59秒跨分、0/25/50/75/100%以及超目标值；全周扫过检查采用保守外接边界。局部数据指针另核对是否撞到所在子表盘边框。静态稿只能证明当前姿态与几何，设备旋转、刷新频率及性能须留给真机验证。

读取天气库时按组件说明确认状态含义，不按数字编号猜天气；生成背景中需要可读的刻度文字应逐项核对；错误字符统一按[一体化背景纠错](background-composition.md#一体化背景的文字与刻度纠错)处理，不强制改用文字图层，也不在用户选定的背景上擅自叠字。
